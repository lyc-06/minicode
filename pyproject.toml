[project]
name = "minicode"
version = "0.2.0"
description = "A lightweight terminal coding agent framework (Python port)"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "rich>=13.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
